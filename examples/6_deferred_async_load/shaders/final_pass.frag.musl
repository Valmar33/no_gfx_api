Data :: struct
{
    gbuffer_albedo: textureid,
    gbuffer_albedo_sampler: samplerid,
}

main :: (uv: vec2 @in_loc(0), data: ^Data @data) -> vec4 @out_loc(0)
{
    // Sample albedo from G-buffer
    albedo := sample(data.gbuffer_albedo, data.gbuffer_albedo_sampler, uv);
    return albedo;
}
